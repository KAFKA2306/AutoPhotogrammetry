This branch exists only as a historical verification branch for Gaussian Splat PLY orientation measurement.

The verification workflow is defined on the matching base branch so pull_request execution can use the nine historical PLY exports without modifying main.
